# Evidence Report

Date: 2026-06-23 Asia/Jakarta

## Local URL

Local dev server: `http://localhost:3021/` during the latest browser-auth run. This is the allowed origin configured for the Privy Testing app.

Hosted deployment: `https://0g-arcade-arena.gabrielaxy.workers.dev`

## Games Implemented

- Grid Four: deterministic two-player alignment game.
- Fleet Duel: deterministic hidden-information fleet search.
- Tile Race: deterministic seeded score race.
- World Cup Draft: deterministic richer draft simulation adapted from 0G World Cup concepts.

## Game Pack Standard Status

All four packs include `manifest.json`, `rules.ts`, `schema.ts`, `agent.md`, `ui/GameView.tsx`, `tests/rules.test.ts`, `fixtures/demo-replay.json`, `assets/cover.svg`, `assets/logo.svg`, `README.md`, and `LICENSE.md`.

All manifest hash fields are concrete SHA-256 hex values and are validated against the current `schema.ts`, `rules.ts`, `ui/GameView.tsx`, `agent.md`, and `fixtures/demo-replay.json` files by `pnpm game:validate`.

PR-based game submission is now proven by `evidence/live-proofs/game-submission-workflow-2026-06-24.json`, generated by `CI=true pnpm game:submission-workflow-check`. The proof verifies that `/submit-game` is pull-request-only, maintainer approval language is present, `docs/GAME_SUBMISSION.md` documents the no-upload workflow, the PR template includes the required checklist/hash fields, Game Pack CI runs on pull requests touching `games/**`, the game tooling scripts exist, every v1 game pack has cover/logo assets, all manifest hashes match current files, and game packs contain no banned secret/network/RCE patterns.

Generated pack receipts:

- `dist/game-packs/grid-four.receipt.json`
- `dist/game-packs/fleet-duel.receipt.json`
- `dist/game-packs/tile-race.receipt.json`
- `dist/game-packs/world-cup-draft.receipt.json`

## Agent Play Status

Platform agents are defined in `src/lib/agents.ts`, and the Worker now exposes a D1-backed qualified agent registry through `/api/agents`. The `/api/rooms/:roomId/agent-move` endpoint attempts 0G Compute when the Worker has `ZEROG_ROUTER_API_KEY`, builds a game-generic prompt from the active manifest/player view/legal moves, validates the returned JSON move against deterministic rules, and stores provider/request proof fields when available. Local Wrangler now binds the project-only 0G testnet Router env from ignored `.dev.vars`; `evidence/live-proofs/router-compute-agent-match-2026-06-24.json` proves a real 0G Router-selected Grid Four agent move from provider `0xa48f01287233509FD694a22Bf840225062E67836`, room completion, and proof indexing with `computeMode=0g-compute`. If the Router fails, the Worker attempts Sarvam before deterministic fallback; `evidence/live-proofs/sarvam-fallback-agent-move-2026-06-25.json` forces a Router network failure, proves `computeMode=sarvam-fallback`, and persists the exact primary 0G error in room compute proof metadata for the UI/proof surfaces.

The project-level external agent skill package is `docs/agent-skills/0g-arcade-player/`. It covers registration, API endpoints, Grid Four, Fleet Duel, Tile Race, World Cup Draft, wager safety, and example interaction transcripts. `CI=true pnpm agent:skill-check` verifies the package against all four game manifests and writes `evidence/live-proofs/agent-skill-package-2026-06-24.json`.

Agent profile images are part of the registration contract. Evidence: `evidence/live-proofs/agent-avatar-api-2026-06-24.json`, generated by `CI=true pnpm agent:avatar-check`, proves `/api/agents` preserves an explicit `avatarUrl` through registration, profile fetch, and qualified listing, and generates an SVG data-URI PFP when a registering external agent omits one.

Playable match routes now persist replay/proof records into browser local storage. `/match/match-grid-four-agent-free-smoke` supports human column clicks followed by deterministic fallback agent replies. Fleet Duel, Tile Race, and World Cup Draft expose deterministic turn controls so their result/proof routes can read the latest local replay. Match-record persistence is covered by a unit test.

Fleet Duel, Tile Race, and World Cup Draft are also proven through the shared Worker/Durable Object room APIs. Evidence: `evidence/live-proofs/multigame-room-api-2026-06-24.json`, generated by `CI=true pnpm room:multigame-check`. The proof creates, joins, starts, and finishes each game in human-vs-human mode, then registers a qualified agent and finishes each game in human-vs-agent mode through `/api/rooms/:roomId/agent-move`. All six rooms produced replay/result hashes, were indexed through `/api/proofs`, and the agent rooms disclosed deterministic fallback mode.

The non-Grid room UI is now proven in the browser for real Fleet Duel, Tile Race, and World Cup Draft rooms. Evidence: `evidence/live-proofs/non-grid-room-ui-2026-06-24.json`, plus per-game screenshots `non-grid-room-ui-fleet-duel-2026-06-24.png`, `non-grid-room-ui-tile-race-2026-06-24.png`, and `non-grid-room-ui-world-cup-draft-2026-06-24.png`. The in-app Browser opened live rooms on `http://localhost:3021`, verified each route game header, shared-room shell, room/game ids, current player, and game-specific legal move controls rendered from Durable Object room state.

All-game human-vs-human browser lifecycle is now proven in `evidence/live-proofs/all-game-h2h-browser-2026-06-24.json`, generated by `CI=true pnpm h2h:all-game-browser-check`. The proof uses two isolated guest browser contexts per game, creates a free human room from each `/games/:gameId` detail UI, joins by room URL, starts from the host UI, clicks visible legal moves until terminal state, verifies `/api/proofs/match-:roomId`, and saves host/guest finished screenshots for Grid Four, Fleet Duel, Tile Race, and World Cup Draft.

The explicit E2E commands now exist and write their own receipts. `CI=true pnpm e2e:local` regenerates the localhost product/start/all-game-H2H/automatch/multigame/agent/submission proof lane and writes `evidence/live-proofs/e2e-local-gate-2026-06-24.json`. `CI=true pnpm e2e:live-0g` refreshes 0G readiness and verifies the live evidence set for authenticated H2H, H2H wager, H2A free/wager Router Compute, current Storage/Chain carry-through, leaderboards, Explorer/proof surfaces, and DA candidate truth boundaries in `evidence/live-proofs/e2e-live-0g-gate-2026-06-24.json`. It does not rerun gas-spending flows unless `E2E_LIVE_0G_RERUN_SPENDING=1` is set.

Local Human-vs-Human room API is now proven for Grid Four through the Worker/Durable Object runtime. Evidence: `evidence/live-proofs/h2h-grid-four-local-api-2026-06-23.json`. The flow created a room on `http://localhost:3021`, joined a second human, started the match, rejected a wrong-turn move, completed a tiny-wager `0.0001 0G` match, and returned replay/result hashes.

Human auto-match is now a real Worker API path instead of a private-room shortcut. Evidence: `evidence/live-proofs/human-automatch-api-2026-06-24.json`. The proof enters one free player into `/api/matchmaking/human`, pairs the second compatible free player into the same room, verifies that the free room auto-starts, confirms the queue clears for the next free player, then repeats the pairing for a tiny-wager room and verifies it remains `ready` until escrow funding. Browser evidence `evidence/live-proofs/human-automatch-ui-2026-06-24.json` and `evidence/live-proofs/human-automatch-ui-2026-06-24.png` verifies the rendered `Find human match` CTA redirects into an active shared-room match.

Focused in-app Browser proof is also saved in `evidence/live-proofs/browser-proof-surfaces-2026-06-24.json`. It verifies the Grid Four game detail route renders the qualified-agent picker and selected-agent CTA, `/leaderboard` renders Global/Grid Four/Free/Wager sections, `/explorer` renders contracts/storage/compute/live match proof panels, and `/proof/match-grid-four-agent-free-smoke` renders proof/replay receipts plus truthful fallback/DA/chain panels. Screenshots are `browser-proof-game-detail-agents-2026-06-24.png`, `browser-proof-leaderboard-2026-06-24.png`, `browser-proof-explorer-2026-06-24.png`, and `browser-proof-proof-page-2026-06-24.png`.

Home product-flow browser proof is saved in `evidence/live-proofs/home-product-flow-2026-06-24.json` with screenshot `home-product-flow-2026-06-24.png`. It opens `http://localhost:3021/` and verifies the requested top navigation, Grid Four featured hero, human room-code controls, testnet wager amount control, agent-play control, and that contract/DA/proof technical details are not on the home CTA surface.

Home start-action browser proof is saved in `evidence/live-proofs/home-start-actions-2026-06-24.json`, with screenshots `home-start-human-room-2026-06-24.png`, `home-start-agent-match-2026-06-24.png`, `home-start-human-wager-room-2026-06-24.png`, and `home-start-agent-wager-room-2026-06-24.png`. It registers fresh qualified free and wager-capable Grid Four agents, clicks the home page human room-code flow into a live `/room/:code`, clicks the selected-agent free flow into an active `/match/:matchId` with `Opponent: agent`, then verifies the home page tiny-wager human and selected-agent flows both stop on escrow room screens before match start.

All-game qualified-agent picker UI is now proven in `evidence/live-proofs/agent-picker-all-games-2026-06-24.json`, with per-game screenshots for Grid Four, Fleet Duel, Tile Race, and World Cup Draft. The proof registers one qualified free-play agent per game through `/api/agents`, opens each `/games/:gameId` route in the in-app Browser, and verifies the agent/human mode controls, qualified-agent panel, seeded agent button, and `Play selected 0G agent` CTA render for every game.

All-game detail start actions are now proven in `evidence/live-proofs/game-detail-start-actions-2026-06-24.json`, with per-game human-room and agent-match screenshots. The proof registers one fresh qualified free-play agent for Grid Four, Fleet Duel, Tile Race, and World Cup Draft, opens each `/games/:gameId` route, verifies cover/logo art, creates a live free human room-code room, then starts a free selected-agent match and verifies the resulting `/match/:matchId` is for the same game.

Local room UI wiring is also smoke-tested. Evidence: `evidence/live-proofs/h2h-grid-four-ui-smoke-2026-06-23.json` and `evidence/live-proofs/ui-browser-mqqtbrjd-match-ui.png`. Headless Chrome opened `/match/match-ui-browser-mqqtbrjd?room=ui-browser-mqqtbrjd`, rendered the shared room, clicked column 1 as `browser-player`, and verified the Durable Object advanced to turn 1 with `wallet-b` as the current player.

Two authenticated Privy browser contexts completed a real local H2H free match. Evidence: `evidence/live-proofs/two-privy-h2h-grid-four-2026-06-23.json`. Account A `test-1032@privy.io` used wallet `0x23761115c5f38ca51f0d425d00DE6E34029239EC`; account B `test-2632@privy.io` used wallet `0xD5F264Ee35815Af73510150302cc52FE686E5f81`. The final room `gr-9rzz` finished after 7 moves with replay hash `0xd7e80012` and result hash `0xe7bf6f52`.

Tiny-wager H2H is now proven end to end on 0G Galileo with a completely browser-clicked move loop. Evidence: `evidence/live-proofs/h2h-grid-four-wager-browser-full-2026-06-23.json`. Room `gr-zqvy` used a `0.0001 0G` wager per player. Wallet A funded in tx `0x0a8d4bd73427f6791dac4b01b4b262f48a83ed205e2b28cb95c0979b60f7b13f`; wallet B funded in tx `0x30753a1fa6bee924d5830d41293e83de8d31734435582f4a8231fc28aced4627`; escrow reached `200000000000000` wei before start. Both authenticated browser contexts clicked all seven moves. The room finished with winner `0x23761115c5f38ca51f0d425d00de6e34029239ec`, replay hash `0x589c7a3b`, and result hash `0xe7bf6f52`. Winner settlement mined in tx `0xe34b656a4541a38d18ea6943cb5c9c8a1d250a6990c14446a276701bbf664969`, and escrow returned to `0`.

D1 proof and leaderboard indexing is now proven locally. Evidence: `evidence/live-proofs/d1-proof-leaderboard-api-2026-06-23.json`. Room `e2e-qvx07g` completed a free Grid Four match, wrote `/api/proofs/match-e2e-qvx07g`, and updated global, game, and free leaderboards. Wager-mode separation is proven in `evidence/live-proofs/wager-leaderboard-api-2026-06-24.json`: room `lbw-mqr19ztg` was funded twice on Galileo, completed through the room API, settled back to the operator, and indexed the winner into global, Grid Four, wager, and Grid Four wager tables while keeping that wager winner out of the free table.

Qualified human-vs-agent free play is now proven locally with deterministic fallback agent moves. Evidence: `evidence/live-proofs/agent-registry-room-api-2026-06-23.json`. Agent `agent-grid-warden-live` was registered as qualified for Grid Four, returned in the qualified-agent list, joined room `agent-qw15vk`, moved through `/api/rooms/:roomId/agent-move`, and appeared in the leaderboard after the finished match.

Agent join policy is now enforced server-side for direct room joins. Evidence: `evidence/live-proofs/agent-wager-policy-api-2026-06-24.json`. The Worker rejects unregistered agents, pending agents, agents disabled for free play, agents unsupported for the room game, agents disabled for wagers, and agents whose `maxWagerWei` is below the room wager; it accepts qualified free agents and wager agents whose cap equals the requested `0.0001 0G` test wager.

Human-vs-agent tiny wager play is now proven on Galileo with live 0G testnet Router Compute. Evidence: `evidence/live-proofs/agent-wager-router-compute-match-2026-06-24.json`. Room `h2a-router-wager-mqs1kt45` registered a qualified Grid Four agent, listed it for a `0.0001 0G` wager, funded both escrow sides on Galileo, started only after escrow funding, played Router-backed agent turns through `/api/rooms/:roomId/agent-move`, received non-fallback `0g-compute` moves, reached a terminal winner, indexed the proof and wager leaderboards for the actual winner/loser pair, and settled successfully in tx `0x8c96abf86a7f4fac55e8e201574487be66e7eb7f3b711e2063886543931e1de0`. The completed room was uploaded to 0G Storage root `0x7aeac238a3e6493e61c9c8bce13988bd9885dbb59954036c8c14943b31daf5a8` and committed to the live Galileo `ArcadeMatchRegistry` at on-chain match ID `9`; create tx `0xb504f095bd513b7f423930b61c3c6b7c378ac653e9c3b8045532b173b5c4fb09`, result commit tx `0x213d718dd8addf75b27f5fea3f5ba75e115c538ebc9a8f09b3165ff498004447`, evidence `evidence/live-proofs/chain-actual-match-h2a-router-wager-mqs1kt45.json`. Historical fallback wager evidence remains in `evidence/live-proofs/agent-wager-match-api-2026-06-24.json`.

Human-vs-agent free play is now live-proven with real 0G testnet Router Compute. Evidence: `evidence/live-proofs/router-compute-agent-match-2026-06-24.json`. Room `router-compute-mqrwdmmf` registered a qualified Grid Four agent, started a free human-vs-agent room, requested an agent move through `/api/rooms/:roomId/agent-move`, received a legal `0g-compute` move from provider `0xa48f01287233509FD694a22Bf840225062E67836`, completed the room, and verified `/api/proofs/match-router-compute-mqrwdmmf` indexed `computeMode=0g-compute`.

Non-Grid human-vs-agent play is also live-proven with real 0G testnet Router Compute and carried through 0G Storage plus Galileo Chain. Evidence: `evidence/live-proofs/multigame-router-compute-api-2026-06-24.json`. Fleet Duel, Tile Race, and World Cup Draft each registered a qualified agent, started a free human-vs-agent room, received one non-fallback Router-selected legal move, completed the room through deterministic legal moves, uploaded the replay to reachable 0G Storage, and committed the result to Galileo on-chain match IDs `6`, `7`, and `8`.

0G Compute output handling is covered by unit tests and live Router evidence. `worker/agent-move-output.test.ts` and `worker/agent-move-choice.test.ts` cover fenced/embedded JSON, missing JSON, invalid JSON shapes, confidence clamping, legal mocked Compute moves, fallback to deterministic legal moves for malformed or illegal mocked Compute responses, and validation of external direct-broker proof metadata for agent-only moves.

External direct-broker proof ingestion is proven locally by `evidence/live-proofs/external-compute-proof-api-2026-06-24.json`, generated by `CI=true pnpm agent:external-compute-proof-check`. It creates a free Grid Four human-vs-agent room, submits a synthetic direct-broker-shaped `0g-compute` proof with the agent move, verifies the room and proof index are marked `computeMode=0g-compute`, and verifies a human move cannot attach external Compute proof metadata.

Wager negative coverage is now proven locally. Evidence: `evidence/live-proofs/wager-negative-api-2026-06-24.json`, `evidence/live-proofs/wager-start-gate-api-2026-06-24.json`, plus Foundry contract tests. The HTTP API rejects unfunded wager starts, unfunded pre-start moves that would otherwise auto-start a room, non-host cancel attempts, join/start after cancellation, wrong-turn moves, illegal columns, and duplicate moves after finish. Foundry now also rejects zero-value wagers and double settlement.

## 0G Integration Status

- Chain: live Galileo smoke deployment and registry writes are verified in `evidence/live-proofs/chain-check-latest.json`; live chain readiness is also verified in `evidence/live-proofs/0g-readiness-latest.json`. The completed browser wager room `gr-zqvy` is committed to the live Galileo `ArcadeMatchRegistry` at on-chain match ID `2`; the historical H2A fallback wager room `h2a-wager-mqr1xs1b` is committed at on-chain match ID `3`; the free live-Router Compute room `router-compute-mqrwdmmf` is committed at on-chain match ID `4`; the live-Router H2A wager room `h2a-router-wager-mqs1kt45` is committed at on-chain match ID `9`; and Fleet Duel, Tile Race, and World Cup Draft Router Compute rooms are committed at on-chain match IDs `6`, `7`, and `8`.
- Storage: live credentials and the 0G Storage turbo indexer endpoint are verified in `evidence/live-proofs/0g-readiness-latest.json`. The completed browser-only wager replay `gr-zqvy`, historical H2A fallback wager replay `h2a-wager-mqr1xs1b`, free live-Router Compute replay `router-compute-mqrwdmmf`, live-Router H2A wager replay `h2a-router-wager-mqs1kt45`, and all three non-Grid Router Compute replays are uploaded to reachable 0G Storage roots. The current H2A Router wager root is `0x7aeac238a3e6493e61c9c8bce13988bd9885dbb59954036c8c14943b31daf5a8`; the non-Grid roots are Fleet Duel `0xfdc8cef344cf0b929509163ab641e17dd2c76c43a724e458cf97eea57cdba786`, Tile Race `0xdbc1eeba3cfc798823209a80d0b456a07de974a91be60e906baf552fcc1153ba`, and World Cup Draft `0x7399a314f84b1f9f5d516d9c6f13417b4bfc76c0d37ad64d319c50d82ef7c72a`. Default static game-pack publish receipts remain local fallback in CI to avoid repeated live storage writes.
- Storage game packs: all four v1 packs were uploaded as live 0G Storage game-pack payloads with reachable roots. Grid Four root `0x81a3504d17ba2fd003069e1c8f62a2b47d20d84618703750e59962e1a2f47e54`, Fleet Duel root `0x64764074cf9142a25147673e160f020dd1ce0d245445ae730d986b106d0289c0`, Tile Race root `0x3edfece3b2e45e10636fb00a4aa8d52b0480648b1c988b8f63c9eabeab4d41d2`, and World Cup Draft root `0x5f4709463db77e8f5133a0dec94d306261a836e5984f25e0021e2c2d7b852220`; evidence is `evidence/live-proofs/0g-storage-game-pack-*.json`. Default `game:publish` still writes local fallback receipts in CI to avoid spending funds on every check.
- Explorer browser evidence `evidence/live-proofs/explorer-all-game-pack-storage-2026-06-24.json` and `explorer-all-game-pack-storage-2026-06-24.png` proves `/explorer` renders all four game-pack storage rows with reachable 0G Storage status.
- Proof artifact bundle: `pnpm 0g:upload-proof-artifacts` uploaded a reachable consolidated payload to root `0x5e6b327f1ad200fd79ecd12aa1471ed7488d7a8da0b906a08aa9a1e41937da51` with tx `0x5c13d3266baa7a2ab9ac3455862279765ec7bd140ea2cf83ffa54525ee6d18b7`. Evidence `evidence/live-proofs/0g-storage-proof-artifacts-2026-06-24.json` verifies the bundle includes the PR-only submission review receipt, share-card metadata for the live `gr-zqvy` match proof, and a non-sensitive H2A fallback agent move transcript.
- Explorer browser evidence `evidence/live-proofs/explorer-proof-artifact-storage-2026-06-24.json` and `explorer-proof-artifact-storage-2026-06-24.png` proves `/explorer` renders the proof artifact root, reachable status, and included artifact types.
- Compute: live on the 0G testnet Router. `pnpm 0g:readiness` proves Chain, Compute, Storage config, and Storage endpoint readiness in `evidence/live-proofs/0g-readiness-latest.json`; the configured router is `router-api-testnet.integratenetwork.work`, the current listed testnet model is `qwen2.5-omni`, and a direct Compute-only probe against that model returns `200`. `evidence/live-proofs/router-compute-agent-match-2026-06-24.json` proves the app used that Router path for a legal free Grid Four agent move, `evidence/live-proofs/agent-wager-router-compute-match-2026-06-24.json` proves three non-fallback Router moves in a tiny-wager Grid Four match, and `evidence/live-proofs/multigame-router-compute-api-2026-06-24.json` proves one non-fallback Router-selected legal move each for Fleet Duel, Tile Race, and World Cup Draft. Direct broker evidence remains useful but optional: `evidence/live-proofs/0g-compute-broker-diagnostic-2026-06-24.json` proves direct provider discovery, while the direct broker ledger path remains blocked because the project wallet has `0.680564052486896229` 0G and cannot create/fund the broker ledger with the current `3` 0G minimum.
- DA: `pnpm 0g:da-candidate` generates an unpublished deterministic DA batch candidate at `evidence/live-proofs/0g-da-batch-candidate-2026-06-24.json`. Batch hash `0x7662efb303bb52394dc4e690c3361b2fc9a316e7aa3eba4613ad86c1d19223e9` covers seven live committed matches, all four game-pack roots, and the proof-artifact bundle. `pnpm 0g:da-readiness` writes `evidence/live-proofs/0g-da-readiness-2026-06-24.json`, which records the official DA Client/Encoder/Retriever requirement and the missing project-local Disperser/Encoder/Retriever/DAEntrance config. `pnpm 0g:da-publish` writes `evidence/live-proofs/0g-da-publication-2026-06-24.json` using a self-contained Node HTTP/2 gRPC client; the current receipt verifies the canonical candidate payload hash and stops before submission because `ZEROG_DA_DISPERSER_GRPC` and `ZEROG_DA_ENTRANCE_CONTRACT` are not configured. Arcade does not claim live DA publication. Explorer browser evidence is saved at `evidence/live-proofs/explorer-da-batch-candidate-2026-06-24.json` and `explorer-da-batch-candidate-2026-06-24.png`.
- Cloudflare operational layer: deployed to Workers with static assets, Durable Object rooms, D1 schema, R2 bucket binding, and KV registry binding.

Generated local publish fallback receipts:

- `dist/publish-receipts/grid-four.publish.json`
- `dist/publish-receipts/fleet-duel.publish.json`
- `dist/publish-receipts/tile-race.publish.json`
- `dist/publish-receipts/world-cup-draft.publish.json`

Generated static proof/replay artifacts:

- `public/proofs/match-grid-four-receipt-proof.json`
- `public/proofs/match-grid-four-receipt-replay.json`
- `public/proofs/match-fleet-duel-receipt-proof.json`
- `public/proofs/match-fleet-duel-receipt-replay.json`
- `public/proofs/match-tile-race-receipt-proof.json`
- `public/proofs/match-tile-race-receipt-replay.json`
- `public/proofs/match-world-cup-draft-receipt-proof.json`
- `public/proofs/match-world-cup-draft-receipt-replay.json`

## Contracts Status

Contracts exist for game registry, match registry, wager escrow, future tournament registry, agent registry, and contributor registry. Foundry tests explicitly cover registering games, duplicate version rejection, free match creation, wager match creation, zero-value wager rejection, winner settlement, double-settle rejection, protocol fee accounting, invalid result submitter rejection, replay hash recording, future tournament result recording, agent rating updates, and contributor credit.

Latest live Galileo chain smoke proof:

- `evidence/live-proofs/chain-check-latest.json`
- Chain ID: `16602`
- Operator wallet: `0x9BD46195661F61a323c3c8C82132dCDE72a3bcbC`
- Contracts deployed: game registry, match registry, wager escrow, future tournament registry, agent registry, contributor registry
- Write proof: register game version, create match, commit result, create/settle wager, record future tournament result, register/update agent, record contributor credit
- Scope boundary: this live chain smoke does not claim 0G Storage upload, 0G Compute execution, or 0G DA publication.
- Browser UI proof: the live chain panel renders on `/explorer` and `/proof/match-grid-four-agent-free-smoke` at `375`, `768`, and `1440` widths with explorer links for the deployed contracts and write transactions.

Actual completed match chain proof:

- `evidence/live-proofs/chain-actual-match-gr-zqvy.json`
- Room: `gr-zqvy`, on-chain match ID: `2`
- Match registry: `0xbfbe80a1907e3cbfc2c2598c8d9fb9665ff0d2a7`
- Create tx: `0xa596e5fe185dbb1d2dcd5acedce2b861fcce416b5c4723ad76128d8116611701`
- Commit tx: `0xe415c99906bb498d64e7ad147680a27d007993bea9f2ef7bb7c1432fa48738d3`
- Scope boundary: this proves the completed browser wager room result and reachable 0G Storage replay hash were committed on Galileo; it does not claim live 0G Compute or DA.

## Completion Audit

`docs/COMPLETION_AUDIT.md` records the current requirement matrix. `pnpm audit:check` verifies required game packs, docs, routes, contracts, proof artifacts, local fallback receipts, truthful fallback labels, and responsive screenshot manifests. It runs inside `pnpm submission:check` after the pack/publish receipts are regenerated.

Cloudflare deployment proof from the prior hosted pass:

- URL: `https://0g-arcade-arena.gabrielaxy.workers.dev`
- Worker version ID: `f0281444-1bf0-4f4f-9361-650946b8202e`
- D1 database: `0g-arcade-arena` (`05b5871d-1882-420b-b978-1cc2af63d6c2`)
- KV namespace: `ACTIVE_GAME_REGISTRY` (`94812c4413e842a48907e79de6b20f4a`)
- R2 bucket: `0g-arcade-game-packs`
- D1 schema applied remotely on 2026-06-23.

Hosted production Privy attempt on 2026-06-24 is blocked, not proven. Evidence:
`evidence/live-proofs/hosted-privy-origin-blocker-2026-06-24.json` and
`hosted-privy-origin-blocker-2026-06-24.png`. The Workers route loads and renders
Login, but Privy blocks the auth iframe/API for the hosted origin, and the hosted
bundle still shows older tournament-path copy rather than the current localhost build.
Rerun with `pnpm hosted:privy-check` after the hosted origin and deployment are fixed.

## Latest Verified Commands

```bash
CI=true pnpm game:validate
CI=true pnpm game:submission-workflow-check # proves PR-only game submission workflow, template, CI, tooling, pack assets, and hash integrity
CI=true pnpm test # 9 files, 18 tests passed
CI=true pnpm typecheck
CI=true pnpm build
pnpm exec wrangler deploy --dry-run --outdir /tmp/0g-arcade-worker-dry-run
CI=true pnpm proof:export
CI=true pnpm infra:check
CI=true pnpm chain:live-check # live Galileo smoke deploy/write proof, writes evidence/live-proofs/chain-check-latest.json
CI=true pnpm chain:commit-actual-match gr-zqvy # commits completed match result/storage hashes to live Galileo MatchRegistry
CI=true pnpm agent:skill-check # verifies the external agent skill package for registration, APIs, all four games, examples, and wager policy
CI=true pnpm agent:avatar-check # proves explicit and generated agent PFPs persist through /api/agents
CI=true pnpm agent:wager-match-check # proves a tiny human-vs-agent wager match with fallback Compute, Galileo escrow funding/settlement, proof indexing, and wager leaderboards
CI=true pnpm agent:wager-router-compute-match-check # proves a tiny human-vs-agent wager match with live Router Compute, Galileo escrow funding/settlement, proof indexing, and wager leaderboards
CI=true pnpm agent:multigame-router-compute-check # proves Fleet Duel, Tile Race, and World Cup Draft each accept one live Router-selected legal agent move and index computeMode=0g-compute
pnpm 0g:upload-room-replay h2a-wager-mqr1xs1b # uploads completed H2A wager replay to 0G Storage
CI=true pnpm chain:commit-actual-match h2a-wager-mqr1xs1b # commits completed H2A wager result/storage root to live Galileo MatchRegistry
pnpm 0g:upload-room-replay h2a-router-wager-mqs1kt45 # uploads completed Router Compute H2A wager replay to 0G Storage
CI=true pnpm chain:commit-actual-match h2a-router-wager-mqs1kt45 # commits completed Router Compute H2A wager result/storage root to live Galileo MatchRegistry
CI=true pnpm human:automatch-check # proves free and tiny-wager human auto-match queue behavior
CI=true pnpm home:product-flow-check # proves the localhost home route is a product-first game/match setup surface
CI=true pnpm home:start-actions-check # proves home CTAs create free human/agent starts and tiny-wager escrow rooms
CI=true pnpm game-detail:start-actions-check # proves every game detail page creates a free human room and starts a free selected-agent match
pnpm hosted:privy-check # records the current production Privy origin blocker or future success evidence
pnpm 0g:compute-broker-diagnostic # proves direct broker/provider discovery and records the current 3 0G ledger funding blocker
pnpm 0g:direct-broker-agent-move # non-autofund guarded proof; after funding, rerun with OG_COMPUTE_BROKER_AUTOFUND=1 to create the broker ledger, submit a live Compute-selected move, complete the room, and index proof computeMode=0g-compute
pnpm 0g:direct-broker-live-pipeline # safe non-autofund orchestrator; after funding, rerun with OG_COMPUTE_BROKER_AUTOFUND=1 to complete broker Compute -> 0G Storage replay -> Galileo result commit
pnpm 0g:upload-room-replay <direct-broker-room-id> # uploads the completed broker-backed room replay, including compute proof metadata, to 0G Storage
CI=true pnpm chain:commit-actual-match <direct-broker-room-id> # commits the broker-backed free match to live Galileo MatchRegistry with wagered=false
CI=true pnpm agent:external-compute-proof-check # proves external direct-broker proof metadata is accepted for agent moves, indexed as 0g-compute, and rejected for human moves
CI=true pnpm wager:start-gate-check # proves unfunded wager rooms cannot start through /start or pre-start /move
CI=true pnpm leaderboard:wager-check # proves tiny escrow-backed wager matches update separated wager leaderboards
CI=true pnpm e2e:local # full localhost browser/API proof lane
CI=true pnpm e2e:live-0g # live 0G evidence gate; set E2E_LIVE_0G_RERUN_SPENDING=1 only when intentionally spending testnet gas
CI=true pnpm audit:check
CI=true pnpm submission:check # aggregate gate, including E2E and audit receipts
node direct route smoke on http://localhost:3021 # all primary app routes returned 200 after clean Worker restart
forge test # 13 contract tests passed
CI=true pnpm game:simulate <each v1 game>
CI=true pnpm game:pack <each v1 game>
CI=true pnpm game:publish <each v1 game> # local fallback receipts
node route smoke against /, /games, /games/grid-four, /play/grid-four/create, /match/match-grid-four-agent-free-smoke, /match/match-fleet-duel-agent-free-smoke, /match/match-tile-race-agent-free-smoke, /match/match-world-cup-draft-agent-free-smoke, /result/match-grid-four-agent-free-smoke, /proof/match-grid-four-agent-free-smoke, /agents, /submit-game, /developers, /leaderboard
node local API smoke against /api/rooms create/join/start/move/get on http://localhost:3021 # completed H2H Grid Four room with replay/result hashes
bundled Playwright/Chrome smoke against /match/:matchId?room=:roomId on http://localhost:3021 # UI click advanced shared Durable Object state
bundled Playwright/Chrome two-context Privy H2H on http://localhost:3021 # both test accounts logged in and finished Grid Four free match
bundled Playwright/Chrome two-context tiny wager on http://localhost:3021 # both Privy wallets funded, escrow-gated start passed, all seven moves were browser-clicked, winner settled on Galileo
```

## Screenshots And Visual QA

Responsive screenshot evidence was captured with the Codex in-app Browser on `http://localhost:3021/`:

- Full-page screenshots: `evidence/screenshots/2026-06-23-responsive/`
- Viewport screenshots: `evidence/screenshots/2026-06-23-responsive/viewport/`
- Breakpoints: `375`, `768`, and `1440`
- Routes: `/`, `/games`, `/agents`, `/leaderboard`, `/explorer`, `/submit-game`
- Result: `18` full-page screenshots and `18` viewport screenshots captured; manifest checks report no horizontal overflow. The latest manifests include the cleaned home route, visual game/agent assets, and the dedicated Explorer technical surface. The home first screen now presents Grid Four as the featured playable game with the same multiplayer setup controls used on game detail pages: play with a 0G agent or humans, choose free/testnet wager mode, then auto-match or room-code human play.

One mobile overflow issue on `/submit-game` was found and fixed by constraining grid columns and wrapping long validation commands. `/polish` was not run for this pass; the user explicitly said polish was not required, so this report uses direct responsive browser screenshots as the visual QA evidence.

In-app Browser smoke on `http://localhost:3021/` passed with Privy test account `test-1032@privy.io` and embedded wallet `0x23761115c5f38ca51f0d425d00DE6E34029239EC`. The Grid Four browser flow opened `/match/match-grid-four-agent-free-smoke`, made one human move, advanced to `Turn: 2` after the deterministic fallback agent reply, and rendered `/proof/match-grid-four-agent-free-smoke` with truthful fallback labels. Screenshot artifact: `evidence/live-proofs/browser-proof-page-3021.png`.

Hosted route/API smoke passed on `https://0g-arcade-arena.gabrielaxy.workers.dev` for the requested primary navigation routes `/`, `/games`, `/agents`, `/submit-game`, `/leaderboard`, `/explorer`, plus `/api/status`. `/api/status` returns `{"chain":"live-galileo-smoke","storage":"local-fallback","compute":"deterministic-fallback","da":"not-configured"}`. In-app Browser rendered the refreshed local routes at `375`, `768`, and `1440` widths with no horizontal overflow; the home page no longer contains the technical chain board, and `/explorer` now contains contracts, transactions, game/result receipts, agent records, and 0G fallback disclosures.

Fresh hosted auth check on 2026-06-24: route load passed, Login was present, but
clicking Login did not authenticate. Browser console evidence shows Privy
frame/API blocking for the Workers origin, and the first viewport still includes
older tournament-path copy. This is recorded as a production-origin blocker, not
a successful auth proof.

Previous M2 Playwright host checks:

```bash
PLAYWRIGHT_CLI_REMOTE=m2worker npx playwright-cli-sessions@latest browser status # No attached Chrome
ssh -o BatchMode=yes -o ConnectTimeout=5 m2worker 'echo m2worker-ok' # timed out
tailscale status | rg 'workers-macbook-pro|m2worker' # offline, last seen 21d ago
```

Report filed: `/Users/gabrielantonyxaviour/.playwright-sessions/.reports/2026-06-22T19-45-24-426-0g-arcade-arena-local-ui-smoke-needed-screenshot.md`

## Known Blockers

- Tiny wager H2H is now live-proven on Galileo with two browser contexts. Wager negative cases are covered locally and at contract-test level, and agent wager/qualification joins are server-enforced by the Worker.
- 0G Storage room replay upload is live-proven by script evidence and is now rendered in the proof/explorer live proof panels. 0G Compute is live-proven through the testnet Router for free human-vs-agent play; the direct broker ledger path remains optional and unfunded.
- 0G DA live publication remains not configured; the deterministic candidate payload/hash is generated and shown in Explorer, and `0g:da-publish` is ready to submit it to a configured Disperser.
- Production Privy browser login on the Workers URL is currently blocked: the hosted origin is not accepted by Privy, and the deployed bundle is stale relative to localhost. Localhost Privy auth remains verified on `http://localhost:3021/`.

## Deployment And Submission Next Step

Next production step is to redeploy the refreshed Worker bundle and re-run hosted Privy auth once the Workers origin is accepted. Optional deeper Compute work is to fund the direct broker ledger and run `OG_COMPUTE_BROKER_AUTOFUND=1 pnpm 0g:direct-broker-live-pipeline`, but the app already has a live 0G testnet Router Compute match, reachable 0G Storage replay, and Galileo result commit.
