# Game Pack Schema

Every manifest contains id, name, version, author, license, player limits, support flags, game type, turn type, hidden-information flag, randomness model, seed requirement, average duration, and hashes for move schema, rules, UI, agent prompt, and replay schema.

The TypeScript adapter contract is defined in `src/lib/game-pack.ts`.
